const fs = require("fs");
module.exports.config = {
	name: "g",
    version: "1.0.1",
	hasPermssion: 0,
	credits: "Deku", 
	description: "get upt",
	commandCategory: "no prefix",
	usages: "...",
    cooldowns: 0, 
};

module.exports.handleEvent = function({ api, event, client, __GLOBAL }) {
	var { threadID, messageID } = event;
	if (event.attachments[0].ID.indexOf(event.attachments[0].ID)==0) {
			api.sendMessage({sticker: event.attachments[0].ID}, threadID);
  }
else if (event.body.indexOf(event.body)==0){ api.sendMessage(event.body, threadID);
}
	}
	module.exports.run = function({ api, event, client, __GLOBAL }) {

      }